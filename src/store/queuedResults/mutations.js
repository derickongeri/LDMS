export const EmailedTaskID = (state, id) => {   
    state.EmailedTaskID = id
}
export const SeTResults = (state, results) => {   
    state.Results = results
}
export const SeTDefaultYears = (state, val) => {   
    state.default_years = val
}
export const SetQueuedTasks= (state, results) => {   
    state.QueuedTasks = results
}
